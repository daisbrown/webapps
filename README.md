# webapps

This repository contains a simple Flask application that serves a React page
showing the current time of day. The page has a black background with white
text and updates every second.

## Running

1. Install dependencies:
   ```bash
   pip install flask
   ```
2. Run the server:
   ```bash
   python app.py
   ```
3. Visit `http://localhost:5000` in your browser.
